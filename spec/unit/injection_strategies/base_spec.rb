require File.join(File.dirname(__FILE__), '../..', 'spec_helper')

describe SSM::InjectionStrategies::Base do
  
  context "when class is passed in" do
    # it "should return the appropriate strategy"
  end
  
  context "when name or symbol is passed in" do
    it "should return the appropriate strategy" do
    end
  end
  
end
